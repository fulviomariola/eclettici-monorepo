# Configurazione Server (Spring Boot girerà qui)
spring.application.name=eclettici-backend
server.port=8082
server.address=0.0.0.0

# Configurazione sicurezza Token JWT
app.jwt.secret=${JWT_SECRET_KEY:LaTuaChiaveSegretissimaEAltamenteSicuraPerIlProgettoEclettici2026!}
app.jwt.expiration-ms=1800000

# Connessione al Database (Porta 5433 mappata da Docker per questo progetto)
spring.datasource.url=${DB_URL:jdbc:postgresql://192.168.1.30:5433/eclettici_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres123}
spring.datasource.driver-class-name=org.postgresql.Driver

# YouTube Data API v3 Configuration
app.youtube.api-key=${YOUTUBE_API_KEY:AIzaSyAuE8xXrXxggxlX8o4ptymxdlRRwdhkWYM}

# Configurazione JPA/Hibernate
## spring.jpa.hibernate.ddl-auto=create
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true

# Per fare debug nella security
logging.level.org.springframework.security=DEBUG